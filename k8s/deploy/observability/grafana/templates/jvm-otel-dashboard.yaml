apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: jvm-otel-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  json: |
{{ .Files.Get "jvm_otel_dashboard.json" | indent 4 }}
